# #759 implementation progress

This file records implementation, not completion of research. Update after every stage/interrupt. Do not check acceptance merely because code was authored.

Documentation review, 2026-09-05: added classic-RTS comparison, integrated C/D/DBG requirements and offline overview. Documentation smoke passed with `AI_OVERVIEW_BROWSER=/usr/bin/google-chrome node docs/ai/759-skirmish-ai/overview.smoke.mjs`: 30 scenario/stage combinations, three keyboard-operated profiles, capacity boundaries, three viewport widths, offline fallback, local links and 121 unique scenario IDs. Desktop/mobile screenshots were visually reviewed; a clipped mobile map label was fixed and the smoke rerun. This was page/specification evidence only; at that review point every runtime stage remained not started.

- Integration branch/worktree: `feature/759-skirmish-ai` in `/home/jernej/.codex/worktrees/7977/fuzzy-waddle`
- Base develop SHA: `de47f482889db30420692bf4406fba463d7db296`
- Research source SHA: `1465f5a8943f19e79041dd9a5355958f214d38f4`; its complete #759 research history is merged into this integration branch. The earlier documentation/skill import is therefore now represented both by the source commits and the merge ancestry.
- Stage 0 PR #792 state/SHA/integration method: open PR #792, source `17789141366e197f388e5ac5ed5cbb64882858e0`; cherry-picked with `-x` as `c57023f2bb0b99e79847bde7bcbb898bc6bab299`, without merging or modifying #792
- Baseline source SHA / fixture manifest version: `de47f482889db30420692bf4406fba463d7db296` / `skirmish-v1`; candidate-derived baseline replacement is prohibited
- Current stage and exact next action: Stage 14 is `implemented_unvalidated` and published. On a new request, begin Stage 15 only from the resume prompt below; it alone executes the deferred validation ladder.
- Execution policy: one stage per user request; Stages 3–14 implement, author tests, perform manual acceptance/Omission/Final Closure audits, commit, push/verify, then stop as `implemented_unvalidated`. Do not execute tests, lint, type checks, builds, formatting validation, browser/runtime smokes, `git diff --check`, or equivalent validation before Stage 15.
- Current actual model/effort: host model/effort metadata was not exposed to this task; Stage 14 recommendation is Terra / xhigh
- Last task-owned commit: `HEAD` (published Stage 14 closure; remote SHA verified before handoff).
- Known integration defects / external blockers: Stages 3–13 are deliberately unvalidated under the current policy; Stage 14 changes shared observation/state/debug contracts and will also require the entire final ladder. Real faction/map skirmishes, command application, research completion/level application, host-transfer/second-match cleanup, UI behavior, and all authored pure/runtime fixtures remain Stage 15 evidence. The release runner fails closed until Stage 15 supplies its isolated candidate/baseline execution layer and every currently-null required runtime fixture. The 18 unrelated Phaser-library TypeScript baseline errors recorded in Stage 1 remain historical context; no validation command has been executed in Stages 3–14.

| Stage | Implementation | Focused gate | Final validation | Commit / paths / next action                                                             |
| ----- | -------------- | ------------ | ---------------- | ---------------------------------------------------------------------------------------- |
| 0     | stage_checked  | passed       | deferred_to_15   | `c57023f2`, evidence published by `273fca68`                                             |
| 1     | stage_checked  | passed       | deferred_to_15   | `39e3c95f`; static correctness and trace/debug foundation                                |
| 2     | stage_checked  | passed       | deferred_to_15   | `1ad2b1ee`; pure contracts, migration, capability and hardening ledgers                  |
| 3     | implemented_unvalidated | authored_not_run | deferred_to_15 | implementation `75dcbc3b`; shared-effect inventory and command/runtime/recovery/debug seams |
| 4     | implemented_unvalidated | authored_not_run | deferred_to_15 | `57fac119`; atomic fair observation, visibility/diplomacy, bounded H7 queries, save-safe memory, catalog and debug |
| 5     | implemented_unvalidated | authored_not_run | deferred_to_15 | `827f3198`; canonical projection, pure/runtime harness, save continuation and repro CLI |
| 6     | implemented_unvalidated | authored_not_run | deferred_to_15 | `4af49ff8`; purpose, claims, fair lanes, difficulty and debug foundation |
| 7     | implemented_unvalidated | authored_not_run | deferred_to_15 | `84db8fbf`; macro proposal/demand persistence, shared-command translation and macro debug |
| 8     | implemented_unvalidated | authored_not_run | deferred_to_15 | `ce20ee36`; access graph, typed routes, transport lifecycle/shared commands/save/debug |
| 9     | implemented_unvalidated | authored_not_run | deferred_to_15 | `541e7e45`; skirmish questions/incidents/squads/mode, shared attack/concede dispatch, debug/save |
| 10    | implemented_unvalidated | authored_not_run | deferred_to_15 | `63c75042`; stable bases, legal placement candidates and expansion intent |
| 11    | implemented_unvalidated | authored_not_run | deferred_to_15 | `9e71a50a`; connected fortification graph and breach recovery |
| 12    | implemented_unvalidated | authored_not_run | deferred_to_15 | `e108319b`; causal recovery, anti-blocking and productive reassignment |
| 13    | implemented_unvalidated | authored_not_run | deferred_to_15 | `a551c4d3`; tactical squads, support, combat accounting and debug workbench |
| 14    | implemented_unvalidated | authored_not_run | deferred_to_15 | `HEAD`; evidence adaptation, research, archetypes, lifecycle and coverage closure |
| 15    | not_started    | not_run      | not_started      |                                                                                          |

For each stage append: implemented symbols and consumers, schema/config decisions, debug fields, authored test/fixture IDs, actual model/effort, pending issues, and exact next action. Allowed implementation states: `not_started`, `in_progress`, `implemented_unvalidated`, `stage_checked`, `validated`, `blocked`. Validation records need command, candidate SHA, fixture version/seed, outcome, artifact path, and any unrelated pre-existing failure. Keep code defects separate from infrastructure blockers.

Use `implemented_unvalidated` for completed Stages 3–14; `authored_not_run` is expected under the latest policy and does not block the next implementation stage after manual audit and verified publication. `stage_checked` is historical for Stages 0–2 only. Stage 15 executes all missing and invalidated checks before anything becomes `validated`. If a shared contract changes, record the affected prior evidence for a Stage 15 rerun; do not blindly trust an old green row.

## Per-stage closure and next-agent handoff

Use the [stage-delivery skill](../../../plugins/fuzzy-waddle-skills/skills/fuzzy-waddle-stage-delivery/SKILL.md). Before the closure commit, append the current stage's acceptance ID -> implemented symbols/consumers -> actual evidence -> status, including owning H/C/D/DBG cases. Record separate Omission and Final Closure audit findings/repairs, exact checks and tested source/diff provenance, actual model/effort, and any final-only deferred evidence.

Record branch, next stage/substep, recommended model/effort from the runbook, blockers and a copyable one-stage prompt. Report final commit and verified remote SHA in the handoff; the next agent verifies the commit containing this ledger. A checked-but-unpushed stage is a publication blocker, not permission to begin the next stage. No self-referential commit SHA is required in its own file.

Documentation/skill maintenance, 2026-09-05: canonical Phaser skill/source routes and reusable stage closure added. No runtime stage is implemented by this maintenance.

## Stage 0 closure — deterministic planner prerequisite

- **Acceptance / implementation:** Reused #792's reviewed fix through `c57023f2`: `filterReachableBuildSpots` waits for every path result before removing unreachable/empty-path candidates; `compareScoredBuildSpots` establishes descending-score then logical-coordinate ordering; `BasePlanner` and `MapAnalyzer` both consume that authority. The change is limited to the prerequisite and its regression coverage—no planner redesign was added.
- **Consumers and provenance:** `BasePlanner.refineAccessibility` now receives a resolved reachable set before assigning `candidateSpots`; `BasePlanner.chooseBuildSite` and `MapAnalyzer.getSuggestedBuildTiles` share the deterministic comparator. The exact upstream source is #792 / `17789141366e197f388e5ac5ed5cbb64882858e0`, cherry-picked with `-x`; baseline predates behavior changes at `de47f482889db30420692bf4406fba463d7db296`.
- **Focused gate:** on candidate source `c57023f2bb0b99e79847bde7bcbb898bc6bab299`, Node `v24.18.0`, pnpm `11.14.0`: `pnpm exec nx test probable-waffle-phaser --runInBand --testPathPatterns='base-planner-selection.spec.ts|world-state-snapshot-manager.spec.ts' --verbose` passed 2 suites / 5 tests. This proves async reachability exclusion and permutation-stable ties; the visibility/ID ordering regression changed by #792 also passed. An earlier `--testPathPattern` invocation performed package bootstrap without a Jest summary and is deliberately not claimed as gate evidence.
- **Omission Audit:** Verified all six #792 files are represented in the cherry-pick; exact call sites use the shared helper; blocked and empty paths are excluded; equal scores have stable x/y tie-breakers; tests exercise both forward and reversed candidate insertion. No duplicate implementation, unrelated runtime mutation, or missing prerequisite behavior found.
- **Final Closure Audit:** Rechecked Stage 0 text against the staged diff and test output. The prerequisite matches #792, carries traceable provenance, pins a pre-change baseline, and has its narrow gate. Full-project lint/build, runtime matches, broad AI scenarios, and baseline comparison remain intentionally deferred to their owning stages/Stage 15.
- **Publication:** this evidence entry is committed/pushed in the Stage 0 closure commit immediately following the prerequisite cherry-pick; its exact remote SHA is recorded in the user handoff rather than self-referentially here.

## Stage 1 closure — static correctness and explainable outcomes

- **Acceptance / static paths:** `isEnemyPlayerWeak` now means own positive strength is strictly greater than the observed enemy strength; MDSL's existing `IsEnemyPlayerWeak` route consumes it. `GatherResources` succeeds only when workers are already genuinely gathering; `StartUpgrade` now delegates to `TechProgressManager.tryStartResearch` and fails when no legal research command can start. `PlayerAiBlackboard.getMostNeededResource` now emits deterministic stock/reservation/income demand rather than always returning `null`.
- **Housing capability:** `SupplyPlanner` resolves positive-`housingCapacity` buildings from the faction's actual foundation builder and `TechTreeService`; `BasePlanner` consumes that capability list, then independently applies its existing production-validator prerequisite/affordability admission. Confirmed ordinary choices are `Olival` for Tivara and `Emberstone` for Skaduwee; `WorkMill` is no longer a housing candidate. An empty builder capability set returns no candidate.
- **Trace/debug and command boundary:** added a bounded 32-entry `AiDecisionTrace` with `ai:<player>:<monotonic-sequence>` IDs, typed reason codes, deterministic serialization, and no Phaser/hidden-world references. `dispatchAiOrder` now returns typed `dispatched`/`dropped` results for non-host, missing command bus, actor ID, or required target ID. Worker assignment counts only dispatched orders. The existing AI debug panel retains its navigation and now offers **Overview & Reasons**, which projects the latest trace event read-only. Trace persistence/replay envelopes and authoritative applied outcomes remain Stage 2/3 work.
- **Fixtures and focused gate:** direct controller static fixtures cover stronger/weaker/equal/zero strength, empty gathering and unavailable research; pure fixtures cover resource demand, trace ordering/retention/serialization, each dispatch-drop reason, both faction housing capabilities, and no housing candidate. On dirty candidate based on `273fca6886ab95783e267641fc5dbb195a4406ec`: `pnpm exec nx test probable-waffle-phaser --runInBand --testPathPatterns='(ai-static-decisions|ai-decision-trace|dispatch-ai-order|supply-planner|player-ai-controller\.agent\.static|base-planner-selection|world-state-snapshot-manager|tech-tree)\.spec\.ts$' --verbose` passed 8 suites / 48 tests. `pnpm exec nx lint probable-waffle-phaser --output-style=static` passed. `git diff --check` passed.
- **Type evidence:** `pnpm exec tsc --project libs/games/probable-waffle/phaser/tsconfig.json --noEmit` reports 18 pre-existing errors in unrelated campaign test fixtures, maps, and `ActorIndexSystem.spec.ts`; after repairing the one new dispatch narrowing error, the final check reported zero paths in every changed Stage 1 module/UI. The focused Jest target compiled and exercised the changed paths successfully. The full-library type gate remains an external baseline blocker for Stage 15 unless the owning work resolves it.
- **Omission Audit:** traced every requested defect from MDSL/legacy caller to outcome and test: polarity, resource demand, gathering, upgrade, housing, trace/reason serialization, missing command addressability, and visible debug projection. Confirmed `BasePlanner` owns affordability/prerequisites separately from housing capability, and added the previously absent Overview navigation route. No random planner redesign, gameplay authority bypass, hidden-data projection, save-schema change, or strategic expansion was bundled.
- **Final Closure Audit:** reviewed all consumers of changed helper signatures and all `dispatchAiOrder` callers; ignored results remain behavior-compatible, while the worker path now treats a drop as failure. The Stage 1 diff is scoped to legacy correctness, bounded diagnostics, UI projection, and regressions. Runtime/real-match behavior, command application acknowledgements, persistence/migrations, pure brain contracts, and broad matrices are deliberately deferred to their owning stages and Stage 15.
- **Publication / next stage:** this evidence is committed and pushed in the Stage 1 closure commit immediately after this update. Next: Stage 2, recommended `gpt-5.6-sol` / high. Resume prompt: `Implement Stage 2 only from docs/ai/759-skirmish-ai/00-start-here.md for #759. Verify Stage 1 remote publication, then define the pure AI contracts, defaults, migration fixtures, capability manifest and gameplay/protocol test seams; run its focused gate, record evidence, commit, push, verify the remote SHA, and stop.`

## Stage 2 closure — pure contracts and capability coverage

- **Acceptance 1 — observation/state boundary:** Added public gameplay contracts for immutable `AiObservationV1` and every required `AiBrainStateV1` slice: strategy/opening, knowledge, bases, economy/production demands, reservations/pending outcomes, squads, transport, fortifications, support, recovery, scheduler/RNG and monotonic identities. Actor observations carry explicit known/unknown logical position/access, owner/diplomacy, effective level/capabilities, queues/cost/supply, resource/growth/service state, effects/zones and mode goals. `assertAiObservationV1` and `assertAiBrainStateV1` reject invalid numeric, identity and persisted-deadline inputs. No Phaser/live-object import crosses this package boundary.
- **Acceptance 2 — actions, plans and hardening:** Added discriminated `AiIntentV1` action families, typed preconditions/claims, demand/plan lifecycles, authority-fenced command/outcome identities, profile configuration and H1 progress/blocker/recovery, H2 dependency/reservation, H3 authority/watermark/backlog, H4 lane-service and H7 query-generation contracts. All persisted deadlines use `{clock: simulation, unit: tick, persistence: save}`. Stable wait-edge identity and guards reject direct self-dependency and duplicate edges.
- **Acceptance 3 — defaults/migration/determinism:** `createAiBrainStateV1` initializes every named slice; `AI_BRAIN_SLICE_POLICIES_V1` assigns each exactly one reducer/default/migration/serializer owner. `adaptLegacyAiSaveToBrainState` is input-only and bootstraps from current-world satisfied opening IDs, so legacy loads do not replay satisfied steps or make the blackboard authoritative. `migrateAiBrainState` preserves checked V1 and explicitly rejects malformed/future schemas. Observation/state canonicalizers sort set-like collections, preserve the ordered RNG sequence, canonicalize outcome order, reject non-finite values, and produce stable serialization/FNV comparison digests.
- **Acceptance 4 — pure reducer/debug:** `PureAiBrainV1.step` validates observation/state ownership and ordered outcomes, evaluates deterministic manager proposals, preconditions, exclusive/resource claims and proposal/acceptance budgets, and returns next state, accepted intents, full decisions/trace and `AiDebugSnapshotV1`. The projector exposes real Stage 2 decisions/recovery plus explicit typed `not_ready` ownership for later panels; bounded causal indexes and completeness flags prevent invented why-not explanations.
- **Acceptance 5 — capabilities/difficulty/DBG-05:** `AiCapabilityCatalogV1` represents runtime-derived effective capabilities without copied balance data. `AI_CAPABILITY_COVERAGE_MANIFEST_V1` covers ownership, attack/heal/spell, economy, production/research, ground/water/air movement, container transport, fortification inputs, conversion and scenario/editor-injected conversion through observation/proposer/command/outcome/save/debug/fixture or an explicit not-applicable reason. Easy/Normal/Hard map from the existing lobby enum to versioned fair cadence/breadth/mission profiles with cheats and intentional errors off. `AiReproBundleV1` separates canonical replay inputs from display metadata; its parser rejects malformed, oversized, future-version, traversal and markup payloads and denies host-confidential runtime bundles to player access. The capture policy is one automatic capture per episode, five retained bundles and 64 MiB/session.
- **Acceptance 6 — executable seams and coverage:** Added real Nx/Jest targets/configs for gameplay and protocol. Gameplay fixtures cover deterministic permutation/digest, state slice policies, legacy/default/future migration, numeric/deadline/dependency guards, all difficulty mappings, accepted/rejected dummy intents, capability coverage, DBG-05 parsing/privacy/capture limits and the H1–H9 ledger. `AI_HARDENING_COVERAGE_V1` maps every H-section to owner/persistence/debug/focused test and H-01–H-32 exactly once. Protocol coverage exercises the existing command/save/replay transport guard seam.
- **Focused gate / provenance:** Candidate is based on `a7dd660f7ca77ac2961e5ea51e71f75a95238f13`; staged Stage 2 code/config patch ID before this ledger update is `47ac2448c42731440d92a23a29a94f5dbe5d20a1`. Node `v24.18.0`, pnpm `11.14.0`. `pnpm exec tsc --project libs/games/probable-waffle/gameplay/tsconfig.json --noEmit`, gameplay `tsconfig.spec.json`, protocol `tsconfig.json` and protocol `tsconfig.spec.json` all passed. `pnpm exec nx test probable-waffle-gameplay --runInBand --verbose` passed 8 suites / 25 tests; `pnpm exec nx test probable-waffle-protocol --runInBand --verbose` passed 1 suite / 5 tests. Both project lint targets passed, formatting was applied to task-owned files, and `git diff --check` passed. Nx daemon/cloud context warnings and deprecated executor notices did not fail a gate.
- **Omission Audit:** Re-read all six Stage 2 requirements plus H1–H9/DBG-05 ownership. Repaired raw persisted deadline fields, implicit slice ownership, missing wait-edge identity, shallow untrusted-bundle validation, mixed display/replay metadata and the absent hardening ledger. Searched all new pure paths for Phaser imports, wall-clock/random calls, unsafe `any`/double assertions and untyped no-op placeholders; none remain. Later-stage debug sections use the required explicit typed `not_ready` state. Verified every new implementation is publicly exported or exercised by a registered test target. No runtime command adapter, live save mutation, copied roster table, second scheduler or strategic manager behavior was introduced.
- **Final Closure Audit / deferred evidence:** Rechecked the repaired diff and immediate public consumers after the final green gate. Stage 2 is contract-complete and compile-ready. Runtime observation/catalog projection, shared applied-command reconciliation, actual save/replay/host integration and live adoption of the pure brain are deliberately owned by Stages 3–6; real-world scenario execution starts with Stage 5, and whole-match/build/UI/baseline validation remains Stage 15. DBG-05's pure parser/privacy cases pass now; its real capture round trip remains Stages 5/13. No current blocker.
- **Publication / next stage:** this ledger is committed and pushed with the Stage 2 closure immediately after this update. Next: Stage 3, recommended `gpt-5.6-sol` / xhigh. Resume prompt: `Implement Stage 3 only from docs/ai/759-skirmish-ai/00-start-here.md for #759. Verify the Stage 2 remote SHA, then connect the pure command identities/outcomes through protocol, server authorization, Phaser application, persistence/replay and debug; migrate effect families in the specified order, implement H3 reconciliation/epoch fencing, run the focused gate, audit, commit, push, verify remote, and stop.`

## Stage 3 closure — shared commands, applied outcomes, and durable effects

- **Acceptance 1 — complete effect ownership map:** Added [the Stage 3 shared-effect inventory](stage-3-effect-inventory.md), tracing production/research/cancellation, construction, move/attack/scout/rally, heal/repair/gather/tend/return, board/unload, cast/autocast, conversion and concession through protocol, server, command bus/runtime application, outcome, save/replay and debug. Added typed `CONSTRUCT`, `CAST_SPELL`, `UNLOAD`, `SET_RALLY_POINT`, and `CONCEDE` payloads only where `ACTOR_ACTION` lacked the required data. Human stop, queue, rally, building placement and spell cursors use the same authority; legacy replay rally remains compatible.
- **Acceptance 2 — identity, idempotence, and lifecycle:** `CommandBusService` now stamps deterministic execution metadata, separates dispatch from application, applies epoch/sequence/command/commitment deduplication, persists bounded watermarks/active multi-actor progress, and emits stable reasons for rejected/applied/active/terminal outcomes. `QueueCommandSystem`, `QueueComponent`, `ProductionComponent`, `ResearchComponent`, `MovementSystem`, `ActionSystem`, and pawn shutdown preserve command context until actual completion/cancellation/failure. Useful repeated units and capacity remain legal because distinct production commands have distinct commitments; construction blocks only the same active `siteKey` and releases it when that site leaves the world.
- **Acceptance 3 — migrated application paths:** Production/research and cancellation revalidate through their existing queue/payment owners; move/combat/support/worker/container orders revalidate through normal actor systems; construction uses `SharedCommandApplicationService` with builder/cost/prerequisite/footprint/collision checks; water-container unloading reports actual passenger links; concession enters `GameModeConditionChecker` rather than destroying actors from AI code. AI manager dispatch success is based on the command receipt, and reviewed strategic AI paths contain no direct actor spawn, owner change, resource mutation, production/research start, spell cast, or cargo unload.
- **Acceptance 4 — deterministic spells and diplomacy:** Projectile gameplay impacts now live in save-owned simulation-tick queues while tweens are presentation only. Caster cooldown/capability, target actor/tile/range, visible enemy versus ally/self/neutral relation, effect polarity, and optional land/water/air domains revalidate at application. Direct and persistent AOE share the team/neutral relation helper. Pending projectile failure on caster destruction is explicit, AOE zone IDs resume above restored IDs, zone data clones deeply, and summon expiries are simulation-tick state captured by save/snapshot/reconnect.
- **Acceptance 5 — cargo and conversion:** `UNLOAD` owns explicit all/subset cargo application and reports actual unloaded IDs; unsafe water unload and missing passengers reject. Existing boarding remains a shared pawn action. `ConvertibleComponent` no longer aborts its scan on an inactive candidate, chooses simultaneous contenders canonically by distance/player/actor ID, emits a conversion observation hook, and persists interval/converted state. AI still cannot call owner mutation directly.
- **Acceptance 6 — brain, debug, replay, server, and recovery:** `AiCommandReconciliation` and `ai-command-outcome-adapter` expose bounded current-epoch outcomes/authority to the Stage 2 pure boundary. The AI debug panel adds a read-only Command Authority view with health, epoch, terminal watermark, pending IDs, and outcome/world links. Replay archives store outcomes and playback detects mismatched, missing, and unexpected terminal outcomes while old archives remain compatible. Protocol guards and the server validate new payloads, safe IDs, execution identity, source/player authority, and host delegation. Saves, snapshots and reconnects restore authority/effect state before the command tail; host migration advances the epoch before host AI resumes.
- **Acceptance 7 / H3 H-06–10:** Missing outcomes become bounded technical faults instead of permission to retry; applied/active work receives a longer terminal window; completion remains authoritative if a resulting actor has disappeared; old epochs are fenced; processed watermarks survive detailed-ID eviction; pending and authority backlogs are bounded; multi-actor commitments settle only after every addressed actor reaches a terminal result. Technical timeout/overflow outcomes deliberately do not erase the authority-side active commitment.
- **Authored tests (`authored_not_run`):** Protocol transport/replay guards, server delegated-AI/source/identity/rally validation, command watermark policy, reconciliation multi-actor/applied/timeout/absent-result/epoch/save/overflow cases, runtime-to-pure outcome ordering, explicit pawn terminal-reason classification, shared player relation, and canonical conversion selection. Stage 15 still owns real-scene vertical slices for production, construction, movement/combat, cargo, spells, concession, replay, reconnect and host replacement.
- **Omission Audit (manual only):** Re-read all seven Stage 3 requirements and H-06–10. Reviewed each changed contract, subscriber, persistence owner and command producer with exact searches. Repairs made during the audit include: separate rally command ownership, filtering rally actors to producers, player-number-zero-safe checks, command-bus server-rejection terminal outcomes, bounded sent-batch bookkeeping, multi-actor terminal aggregation persisted across restore, monotonic processed watermarks, construction-site key cleanup/rebuild legality, pawn/queue/caster destruction outcomes, stable restored AOE IDs, detection of unexpected extra replay outcomes, and an explicit fail-closed pawn terminal-reason policy so missing capabilities/unreachable work cannot be reported as success. No validation command was executed.
- **Final Closure Audit / deferred evidence:** Manually reviewed the final command/application/recovery paths and documentation policy. Stage 3 is `implemented_unvalidated`, not `stage_checked`; every test, lint, type, build, format, browser/runtime smoke, replay run and H-case execution remains explicitly deferred to Stage 15. Current host model/effort metadata was not exposed; recommended owner was `gpt-5.6-sol` / xhigh. No known implementation blocker is being waived, but compile/runtime correctness is unproven until final validation.
- **Publication / next stage:** Stage 3 implementation commit `75dcbc3b` plus this publication-ledger follow-up are pushed to `origin/feature/759-skirmish-ai`; the verified remote tip is recorded by the delivering task. Next requested work is Stage 4 only, recommended `gpt-5.6-terra` / xhigh. Resume prompt: `Implement Stage 4 only from docs/ai/759-skirmish-ai/00-start-here.md for #759. Verify the published Stage 3 SHA and read stage-3-effect-inventory.md; then implement atomic fair observation, knowledge/age/confidence, diplomacy and capability/catalog projection with H7 query isolation. Author all owning tests but do not run tests, lint, type checks, builds, formatting validation, browser/runtime smokes, git diff --check, or any equivalent validation. Perform manual acceptance/Omission/Final Closure audits, record implemented_unvalidated evidence, commit, push, verify the remote SHA, and stop before Stage 5.`

## Stage 4 closure — atomic fair observation and bounded knowledge

- **Acceptance 1 — one atomic permitted observation:** `AiObservationPipeline` captures indexed eligible actors at one simulation tick, canonicalizes the complete observation before publication, pairs it with a same-generation runtime capability catalog, and refuses an older generation at its publication fence. `PlayerAiControllerAgent` awaits this refresh and the legacy snapshot update; there is no Stage 4 fire-and-forget refresh. The new pipeline is saved through `PlayerAiController` but holds no live actor reference in persistence.
- **Acceptance 2 — fair visibility and diplomacy:** `AiObservationVisibilityPolicy` uses the existing player relation helper plus the observing player's owned `VisionComponent` sources. Normal skirmish excludes hidden actors and distinguishes self, ally, neutral and enemy; explicit `revealed`/`omniscient-ai` campaign policies remain separately named. The legacy live-target snapshot now uses this same policy and only admits legal enemy targets, so an ally can be visible support without becoming a target.
- **Acceptance 3 — evidence, memory, coverage and threat:** visible contacts receive stable `evidence:contact:<actorId>` identities. Loss of sight removes their live projection and leaves only a bounded, decaying `last_seen` hypothesis until its deterministic expiry. Saved contact normalization rejects malformed state, canonicalizes duplicate evidence by most recent sighting, caps the cursor debt and clones positions. Observation now carries explicit owned-vision coverage seeds and a canonical visible-versus-remembered threat summary; repeated reads replace a contact rather than adding corroboration.
- **Acceptance 4 — legal runtime/map/capability facts:** projections use runtime effective definitions/levels, component capabilities, source/drop-off/owned queue state, status IDs, owned permitted zones, map bounds, known visible dynamic obstacles and current faction tech-tree roster. The catalog never enumerates the global tech graph, preventing opponent-faction roster leakage. Ground/water/air endpoint checks route through `canActorTraverseTile` and the existing height, occupancy and terrain rules; optional access work reports `unknown`, `blocked`, `service_failed` or `not_ready` rather than inventing access. Current skirmish mode has no objective projection API, so its legitimate objective list is explicitly empty; campaign objective projection stays its owning system until a shared runtime objective seam exists.
- **Acceptance 5 — H7 scheduling and debug:** only four optional access products are selected per generation through a deterministic saved round-robin cursor; navigation invalidations increment bounded debt without delaying the atomic local-defense facts. The debug panel consumes controller-committed metadata only and shows policy, generation, age, visible/remembered counts, unknown facts, access status, invalidation debt, threat evidence and scout coverage; it makes no live hidden-world query.
- **Authored fixtures (`authored_not_run`):** `ai-observation-pipeline.spec.ts` covers stale-generation fencing, C3/C-04 confidence/expiry, `SCOUT-05` permutation-equivalent digest, H-19/H-20 bounded round-robin/invalidation behavior, and deterministic memory restore without live handles. `ai-observation-visibility-policy.spec.ts` covers normal hidden contact denial, explicit campaign reveal/omniscience, allied-support versus enemy targeting and ownership reclassification. These own the Stage 4 fairness, stale-generation, ownership, coordinate-memory and visibility seams; real-scene equivalents, save/reload and all H7 long-run cases remain for Stage 15.
- **Omission Audit (manual only):** Re-read all seven Stage 4 requirements, retained acceptance, `SCOUT-05`, H-19/H-20/H-22 and the Stage 3 effect inventory. Repaired the initial global tech-graph catalog enumeration, strengthened save normalization/deep copying, added explicit coverage/threat/age/unknown debug, replaced generic tile checks with the existing actor traversal rule, and corrected the legacy targeting fallback. No private enemy queue/research/cooldown, hidden actor ID, ally target or UI live-world query is introduced. No validation command was executed.
- **Final Closure Audit / deferred evidence:** Manually traced observation capture through controller scheduling, save/load, debug, legacy target refresh, policy, faction catalog and bounded navigation products. Stage 4 is `implemented_unvalidated`, not `stage_checked`: tests, lint, type checks, builds, formatting validation, browser/runtime smoke, `git diff --check`, real scenarios and final code review remain deferred to Stage 15. Current host model/effort metadata was not exposed; the documented owner recommendation is `gpt-5.6-terra` / xhigh. No implementation blocker is being waived; runtime/compile correctness is deliberately unproven until final validation.
- **Publication / next stage:** this ledger is committed and pushed with the Stage 4 closure immediately after this update. Next: Stage 5 only, recommended `gpt-5.6-sol` / high. Resume prompt: `Implement Stage 5 only from docs/ai/759-skirmish-ai/00-start-here.md for #759. Verify the published Stage 4 SHA, then build and register the deterministic harness bootstrap/oracles/fixture runner and capture-replay comparison seams from the Stage 5 runbook. Author all required tests and fixtures but do not run tests, lint, type checks, builds, formatting validation, browser/runtime smokes, git diff --check, or any equivalent validation. Perform manual acceptance/Omission/Final Closure audits, record implemented_unvalidated evidence, commit, push, verify the remote SHA, and stop before Stage 6.`

## Stage 5 closure — deterministic harness, continuation and reproduction

- **Acceptance 1 — canonical authoritative comparison:** `StateHashService.captureAuthoritativeProjection` now owns one reusable normalized projection. The existing multiplayer digest remains the legacy DJB2 surface and excludes harness-only system state; `digestAiWorldProjectionV1` separately covers canonical actor AI continuations, controller saves, command authority, zones, summon expiry and RNG. The projection and scenario comparators retain the first differing tick and nested normalized actor/AI field rather than inferring from hashes alone.
- **Acceptance 2 — save/restore completeness:** `PlayerAiController` persists a validated canonical `AiBrainStateV1`, command reconciliation, permitted observation memory and a bounded cadence frontier. Existing Stage 3 save owners remain authoritative for production/research queues, command outcomes, cargo, spell cooldown/effects/zones/pending impacts, summon expiry and conversion. Stage 5 adds stable gather current/previous source IDs, crop growth/tender IDs, restored conversion construction and actor serialization. Migration rebuilds state through stable IDs and completed opening checkpoints; the pure brain is deliberately not live until Stage 6, preventing a second scheduler or duplicate opening/production path now.
- **Acceptance 3 — pure fixtures and independent oracle:** `AiScenarioV1`, its canonical builder, reciprocal subject/control metadata, discriminated semantic assertions, numeric deadline/work bands and non-vacuity requirements form the fixture contract. `runPureAiScenarioV1` emits exact ordered intents, trace, prior/current AI and command/trace digests plus semantic outcomes. `findFirstAiDifferenceV1` and the oracle do not call planner scoring. Authored self-tests cover three identical command/AI/world results, a nested AI divergence, deliberately broken expectations and reciprocal controls.
- **Acceptance 4 — real runtime boundary:** `AiRuntimeScenarioBridgeV1` is construction-gated by explicit developer-test mode, bounded to 20,000 fixed ticks, emits normal Phaser update events, waits for controller-committed observations, captures the shared authoritative projection and restores only through the controller save seam. `runAiRuntimeScenarioV1` sequences sorted checkpoints. The authored command/save-continuation case uses the real `CommandBusService` authority/dedup path and proves a restored command cannot apply twice; full booted-scene execution remains `authored_not_run` for Stage 15.
- **Acceptance 5 — matrix/reproduction tooling:** `tools/ai/run-skirmish-matrix.mjs`, the gameplay Nx target and `ai:skirmish-matrix` script provide stage/single scenario, seed and pure/runtime filters, capture replay, bounded breakpoints, comparison, explicit fixture export, retained reports and nonzero missing-coverage behavior. Provenance includes the candidate SHA, tracked and untracked dirty-source digest, fixture digest and scenario context. Invalid references/markup/version/size/history/digest/source/config inputs fail closed. The release path cannot report success before Stage 15 installs the documented isolated baseline execution layer.
- **Acceptance 6 — pinned baseline/catalog:** `skirmish-v1.json` registers all 121 stable C/D/DBG/H/SEQ and strategic IDs with stage/driver ownership; only DBG-01/03/04/05 are runnable Stage 5 bootstrap fixtures, and null future-stage fixtures deliberately fail mandatory release coverage. `baseline-v1.json` pins `de47f482889db30420692bf4406fba463d7db296`, records detached isolated worktrees and revision-local lockfiles, and forbids candidate-generated expectations. Baseline support reports pure and runtime compatibility independently; `baseline-report-v1.json` records commit, contract versions, map, seed, side, faction and rules for all four Stage 5 debug rows.
- **DBG-01/03/04/05 and authored evidence (`authored_not_run`):** Decision/runtime capture envelopes separate permitted-player from host-confidential data; incomplete history cannot claim exact replay; the offline decision stepper validates integrity and stops once at a named boundary; comparisons distinguish changed inputs from same-input divergence and report first tick/path; fixture export uses an explicit create-only destination. Authored/extended files are `ai-scenario-harness.spec.ts`, `ai-repro-cli.spec.ts`, `ai-runtime-scenario.spec.ts`, `authoritative-state-projection.spec.ts`, `actor-manager-ai-save.spec.ts` and `canonical-ai-serialization.spec.ts`. No test result is claimed.
- **Omission Audit (manual only):** Re-read all six Stage 5 requirements, retained acceptance, scenario schema and DBG obligations. Repaired untracked-source omission in dirty provenance, coarse mixed-driver baseline labeling, unvalidated offline stepping, missing exact trace/prior digest output, absent reciprocal control metadata, vacuous upper-only work counters, shallow AI first-diff fields, and runtime comparison limited to pure expected results. Verified the multiplayer digest input string remains compatible, future fixture holes fail release, and no wall-clock threshold, live hidden-world read, ordinary client command, second simulator or candidate-generated baseline was introduced. No validation command was executed.
- **Final Closure Audit / deferred evidence:** Manually traced the canonical projection, save fields/stable-ID restoration, pure oracle/report, runtime bridge, command continuation, repro safety and baseline manifest to their owning consumers. Extending the Stage 2 legacy migration contract invalidates that test file's earlier focused evidence; its new completed-opening case and the prior cases must rerun in Stage 15. Stage 5 remains `implemented_unvalidated`: every authored test, matrix invocation, lint/type/build/format check, browser/runtime smoke, real three-run digest comparison and baseline execution is deferred to Stage 15 by explicit user policy. Current host model/effort metadata was not exposed; documented recommendation was `gpt-5.6-sol` / high. The fail-closed Stage 15 release executor and owning-stage fixture bodies are intentional next-stage work, not silently passing coverage.
- **Publication / next stage:** this ledger is committed and pushed with the Stage 5 closure immediately after this update. Next: Stage 6 only, recommended `gpt-5.6-terra` / xhigh. Resume prompt: `Implement Stage 6 only from docs/ai/759-skirmish-ai/00-start-here.md for #759. Verify the published Stage 5 remote SHA and reuse its scenario/repro contracts; then implement the shared purpose/goal/stance utility algorithm, demand and reservation lifecycle, deterministic arbitration, real difficulty/archetype wiring, retry/watchdog and H1/H2/H4 supervisor with D-01–03, DBG-02, PRO-05 and owning H/STRAT fixtures. Author but do not run tests, lint, type checks, builds, formatting validation, browser/runtime smokes, git diff --check, or equivalent validation. Perform manual acceptance/Omission/Final Closure audits, re-evaluate every Stage 6 requirement, commit, push, verify remote, and stop before Stage 7.`

Maintenance checks recorded before runtime implementation: all six skills passed the bundled skill schema validator; `node tools/skills/check-index.mjs` resolved six skills, nine reference links, 45 indexed source routes and four project definitions; `node --test tools/skills/check-index.test.mjs` passed both regressions (missing/escaping paths, target/name drift). The offline overview smoke passed at that historical point. Stage-policy review covered successful closure, failed required check, rejected push, already-satisfied prerequisite and interrupted resume; these were instruction reviews, not executed AI matches.

## Stage 6 closure — shared decision, profile and progress foundation

- **Acceptance 1–3 — persistent purpose, fair arbitration and claims:** `planAiStage6V1` is now the common pure pre-arbitration reducer. It retains stance/commitment state, sorted manager batches, all five fair service lanes, expiring provisional leases, bounded overdue-progress blockers and deterministic cycle release. `PureAiBrainV1` seeds each decision from persisted claim subjects and records accepted claims as save-safe provisional reservations; an identical actor/site/queue claim therefore cannot win a later decision merely because the controller stepped again. Domain proposals remain manager-owned for Stages 7–14 rather than adding a second command authority.
- **Acceptance 4 — real difficulty/archetype provenance:** `PlayerAiController` resolves the existing lobby difficulty to `AiProfileConfigV1`, uses its decision interval for actual controller cadence, persists the selected difficulty with the brain, deterministically selects/saves an opening archetype and exposes both at its committed debug boundary. Profiles retain no cheats or visibility/rules change.
- **Acceptance 5–7 / DBG-02 / H1,H2,H4:** the controller commits the pure brain only after the fair Stage 4 observation refresh and consumes the Stage 3 outcome/authority bridge; it does not treat dispatch as completion. The debug projection now carries bounded rejected why-not facts and distinguishes absent committed planner data from a recorded rejection. The existing debug label shows committed purpose, profile, commitment window and next actions without reading hidden live-world state. Stage 6 authored `ai-stage-6-planner.spec.ts` covers deterministic lane consideration plus expiring versus applied reservation behavior; existing pure-brain tests continue to own reproducible conflict winner/loser coverage. H-01–05/H-11–15 and PRO-05 fixtures are authored as coverage requirements for Stage 15 and are not claimed passed.
- **Omission Audit (manual only):** Re-read Stage 6, shared demand/idempotence and H1/H2/H4 obligations. Repaired the initial use of a strategy slice where the opening lifecycle was required, persisted physical claim subjects (not only claim IDs), separated expired provisional work from applied spending, and avoided an AI-only command channel. Same-type duplicates remain legal: only the same physical claim/demand commitment is fenced; Stage 7 owns evidence-backed demand counts and real production/placement proposals.
- **Final Closure Audit / deferred evidence:** Manually traced profile selection from lobby definition through controller cadence/save/brain/debug, outcome authority through the Stage 3 bridge, and every pure planner write through canonical serialization. Stage 6 is `implemented_unvalidated`: no tests, lint, type checks, builds, formatting validation, browser/runtime smoke, `git diff --check`, real scenario matrix or replay roundtrip was executed, by explicit user policy. Runtime domain behavior remains intentionally transitional until Stage 7 migrates demand/economy/production proposals; this stage establishes the shared arbiter rather than claiming finished macro play.
- **Publication / next stage:** this ledger is committed and pushed with the Stage 6 closure immediately after this update. Next: Stage 7 only, recommended `gpt-5.6-terra` / xhigh. Resume prompt: `Implement Stage 7 only from docs/ai/759-skirmish-ai/00-start-here.md for #759. Verify the published Stage 6 SHA, then migrate opening, economy, supply, placement and production managers into the Stage 6 pure proposal/demand/reservation boundary. Implement justified duplicate capacity/units, actual command translation and H5/PRO cases. Author but do not run tests, lint, type checks, builds, formatting validation, browser/runtime smokes, git diff --check, or equivalent validation. Perform manual acceptance/Omission/Final Closure audits, record implemented_unvalidated evidence, commit, push, verify the remote SHA, and stop before Stage 8.`

Tooling follow-up, 2026-09-06: direct ESLint 9 invocation failed because this checkout has legacy .eslintrc.json rather than flat config; its root overrides do not cover standalone .mjs tools. The two new tools instead receive explicit `--no-config-lookup --no-ignore --global process --global console --rule 'no-unused-vars:error' --rule 'no-undef:error'` checks and Node syntax checks, plus their runtime regressions. This is focused tool coverage, not a passing repository-wide lint run. No lint configuration or game code was changed.

## Stage 7 closure — macro proposal, capacity and composition boundary

- **Acceptance / implementation:** Added `AiStage7MacroManagerV1`, a runtime-catalog-paired pure proposer that persists faction opening checkpoints, supply buffer demand, and first-squad composition demand in `AiBrainStateV1.economyProduction`. It derives both faction worker bootstrap, housing, first-producer and sustainable-food checkpoints from the committed world, and only proposes a producer/build action where the current faction catalog exposes a legal producer/builder. The intended opening aliases remain Tivara `Sandhold -> TivaraWorker`, `Olival`, `AnkGuard` and Skaduwee `FrostForge -> SkaduweeWorker`, `Emberstone`, `InfantryInn`; no roster symmetry or invented unit is introduced.
- **Duplicate and recovery behavior:** Durable demand IDs and Stage 6 physical site/queue/effect leases fence repeated unobserved construction or training. Demand remains count-based: the manager computes the exact supply deficit (therefore can admit more than one house when necessary), and never applies a per-type uniqueness rule. A missing producer can use a legal builder fallback; a missing worker can use a legal main producer. Existing/observed capacity satisfies the checkpoint and prevents a fresh request. The Stage 15 H5/PRO fixtures must still prove late callbacks, destroyed capacity, throughput-driven second producers/drop-offs and 100-step no-spam behavior in real worlds.
- **Composition and execution:** First-squad demand is six military population with a deterministic 60/40 frontline/ranged target, stable role/type tie-break, queue look-ahead cap and a legal catalog producer. Repetition of a useful unit is explicitly legal while a role deficit remains. `PlayerAiController` now gives accepted `produce` and `construct` macro intents to `dispatchProductionCommand`/`CommandBusService` via `ActorIndexSystem`, the same authoritative path as human commands; unsupported future tactical intents do not receive an AI-only mutation route. Stage 3 reconciliation observes those shared command outcomes.
- **Persistence/debug/contract consumers:** `AiManagerProposalV1.statePatch` creates a narrow deterministic commit seam so the macro manager cannot mutate prior state. `PureAiBrainV1` applies the ordered state projection at the decision boundary, then records accepted leases. `projectAiDebugSnapshot` marks the build-order, production/composition and economy sections ready and names accepted macro reason codes. The production debug panel shows committed opening step, demand fulfillment and reservation count from saved brain state rather than recomputing from live hidden data.
- **Authored evidence (`authored_not_run`):** `ai-stage-7-macro-manager.spec.ts` covers a missing-worker legal producer and fulfilled supply control. It is the initial pure H5 / ECO-07 / ECO-08 / PRO-04 seam. The remaining named Stage 7 fixtures—ECO-01–08, PRO-01–07, STRAT-06/10, C-01–03, D-04 and H-11/13/16/17/18 including both actual factions—remain authored coverage obligations for the fixture matrix and runtime bridge, to be completed/executed by their owning implementation/final Stage 15 work.
- **Omission Audit (manual only):** Traced committed observation -> generation-matched faction catalog -> macro proposal -> Stage 6 arbitration/leases -> brain save/debug -> `ActorIndexSystem` -> shared command bus. Repaired the initial producer-only opening path by adding a legal builder fallback, exported the new gameplay owner, and kept construction count-based rather than type-unique. No raw faction balance table, hidden opponent fact, direct runtime mutation, ship grant, transport counting as combat, or test execution was introduced. Worker allocation, delivered-income measurement, deposits/fields/tending, resource-cost claims, throughput lead-time simulation, queue cancellation/refund policy, and broad producer redundancy need the richer runtime adapters planned in later macro/hardening stages; they are explicit deferred implementation limits, not claimed complete.
- **Final Closure Audit / deferred evidence:** Manually reviewed the staged call path and state ownership, actor-ID addressability, command receipt boundary, save canonicalization and debug-only projection. This stage is `implemented_unvalidated`: no tests, lint, type checks, builds, formatting validation, browser/runtime smoke, `git diff --check`, or scenario matrix was run under the user policy. The focused gate and all integration evidence remain deferred to Stage 15. Host model/effort metadata was not exposed; the documented recommendation is `gpt-5.6-terra` / xhigh.
- **Publication / next stage:** This ledger is committed and pushed with the Stage 7 closure immediately after this update. Next: Stage 8 only, recommended `gpt-5.6-sol` / high. Resume prompt: `Implement Stage 8 only from docs/ai/759-skirmish-ai/00-start-here.md for #759. Verify the published Stage 7 SHA, then implement the versioned multi-domain access graph and full transport lifecycle through the existing shared command/outcome seams. Author but do not run all Stage 8 access/boat/flying-container fixtures, lint, type checks, builds, formatting validation, browser/runtime smokes, git diff --check, or equivalent validation. Perform manual acceptance/Omission/Final Closure audits, record implemented_unvalidated evidence, commit, push, verify the remote SHA, and stop before Stage 9.`

## Stage 8 closure — multi-domain access and transport lifecycle

- **Acceptance 1–2 / DOMAIN-01–04,06 / RAID-03:** Added immutable `AiAccessGraphV1`, stable compact ground/water/air regions, deterministic clearance/elevation links, shore/air transfer points, generation/revision metadata and a 512-cell continuation adapter. Route queries return direct movement/firing, water transport, air transport, air/naval objective, pending or reasoned-impossible results with ordered nodes/transfers, integer costs and required assets. Clearance-specific links reject a formation at a one-tile bottleneck while admitting a single-tile mover. The transitional legacy attack action now asks the committed graph first, so disconnected ground targets cannot create an impossible direct-order loop.
- **Fair topology/cache boundary / H-19–21:** The adapter hashes only owned or currently permitted visible collider/navigable topology, including direction-port state; the global runtime height graph is sampled only at admitted navigable surfaces so hidden walls cannot invalidate or shape the AI graph. A changed permitted revision rebuilds in bounded chunks while the previous graph is explicitly `pending`. Static terrain uses existing navigation/water authorities. Stage 4's fair optional-query lane remains independent, and typed pending/service-failure reasons do not become terrain conclusions.
- **Acceptance 3–6 / H8,H-29:** Added `AiStage8TransportManagerV1` and `createAiTransportPlanV1` with persisted proposed/reserving/gather/rendezvous/boarding/transit/landing/unloading/regroup/handoff/recover/cancel/fail phases. Stable manifests own passengers, declared seat cost, carriers, escorts, transfer time slots and destination; conflicts cancel deterministically. Island establishment requires an indispensable builder plus protection. Departure requires all indispensable members and 75% of required seats, boarding starts with 600 ticks, travel uses `max(600, 2*estimate)`, and route/pickup/landing generation and observed threat are rechecked before travel/unload. Missing actors, carriers, failed shared outcomes, pending routes and unsafe landings enter tick-bounded retries capped at three attempts. Confirmed unload plus arrival in the intended access region is required before handoff clears passenger/carrier ownership.
- **Capacity/shared execution:** Current mobile water/air containers are selected by actual observed container state; stationary deposit containers cannot qualify. Missing seats create one operation-linked capacity demand and at most one correlated legal production proposal at a time, including a catalog-declared producible carrier route when zero carriers are complete. `PlayerAiController` translates accepted produce/construct/move/board/selective-unload intents through `CommandBusService.dispatchAi`; the ordinary command bus owns sequence/epoch/application outcomes and the operation consumes those identities. Selective unload addresses only the mission manifest, and runtime cargo death/survival remains the existing `ContainerComponent` authority—no teleport or rescue path was added.
- **Persistence/debug/schema decisions:** The V1 transport lifecycle remains optional only to migrate earlier empty/preparatory V1 saves; every Stage 8 plan field is canonicalized and structurally checked, including deadlines, capacities, manifests, seats and assignments. Observations now expose owned container/cargo state and an optional committed access graph with canonical nodes/links/transfers. The debug snapshot and **Routes & Transport** panel show committed graph revisions, operation phase, route, capacity, deadlines, recovery/cancel cause and recorded pickup/landing overlays. The overlay only converts recorded tiles to display coordinates and never starts path work. The lightweight Phaser source index now points directly to the access adapter and cargo owners and records the global-height-graph fairness trap for later agents.
- **Authored evidence (`authored_not_run`):** `ai-access-graph-v1.spec.ts` covers permutation-stable graph/shore output, disconnected-ground water routing, explicit impossible results, unknown/pending/service-failure isolation, producible carrier capacity and H-21 narrow-corridor clearance. `ai-stage-8-transport-manager.spec.ts` covers an actual-`ObjectNames.CommonBoat` multi-tile water lifecycle through handoff/release, dead indispensable-member exhaustion, duplicate ownership cancellation, unsafe landing recovery and canonical save identity during boarding/transit/unloading. `ai-stage-8-domain-fixtures.spec.ts` pins the real CommonBoat water/container definition and separately marks an unregistered synthetic flying container that uses generic ground pickup/drop points without shore assumptions. The runtime H-19–21/H-29/DOMAIN/RAID/SEQ-05 rows remain required final fixtures; their manifest entries deliberately stay null rather than falsely labeling pure Jest cases as complete runtime scenarios.
- **Omission Audit (manual only):** Re-read every Stage 8 step, retained acceptance, H7/H8 and H-19–21/H-29. Repairs made during review: preserved missing indispensable members instead of trimming them away; counted declared seat costs rather than actors; allowed a legal catalog production path to create transport demand; bounded pending-route retries by ticks; revalidated graph generation before transit; used selective unload; split access regions across clearance/elevation and linked them with bottleneck capacity; excluded hidden height surfaces from topology; and included navigable direction state in invalidation. Same-type carriers remain legal when the demand needs capacity. No faction receives a boat, synthetic flyer, hidden topology, AI-only command authority, teleport or blanket sea-region reservation. No validation command was executed.
- **Final Closure Audit / deferred evidence:** Manually traced permitted observation -> graph/cache -> typed query -> saved operation -> Stage 6 arbitration/claims -> shared command/outcome -> confirmed cargo/regroup -> ownership release -> debug/save serialization. Stage 8 is `implemented_unvalidated`, not `stage_checked`: all authored tests, lint, type checks, builds, formatting checks, browser/runtime smoke, `git diff --check`, real CommonBoat movement/boarding/unload, save/reload next-command equivalence, topology-storm budget, transport death, actual duty after handoff and transport reuse remain deferred to Stage 15 by explicit user policy. The added graph/link/container/intent/state contracts invalidate prior compile/serialization evidence and require Stage 2–7 regression reruns in Stage 15. Stage 9 owns purposeful scouting/combat objective creation and will seed these legal route/transport plans; Stage 8 does not invent a target. Host model/effort metadata was not exposed; the documented owner recommendation is `gpt-5.6-sol` / high.
- **Publication / next stage:** This ledger is committed and pushed with the Stage 8 closure immediately after this update. Next: Stage 9 only, recommended `gpt-5.6-terra` / xhigh. Resume prompt: `Implement Stage 9 only from docs/ai/759-skirmish-ai/00-start-here.md for #759. Verify the published Stage 8 remote SHA, then implement question-driven legal scouting, threat incidents, persistent domain-aware squads/objectives, defense/attack/reserve/reinforcement missions, transport coordination, conversion/allied behavior, authoritative recoverability/concession and causal debug timelines through the Stage 6–8 seams. Author all Stage 9 C/D/H/SEQ and land-loop fixtures, but do not run tests, lint, type checks, builds, formatting validation, browser/runtime smokes, git diff --check, or equivalent validation. Perform manual acceptance/Omission/Final Closure audits, record implemented_unvalidated evidence, commit, push, verify the remote SHA, and stop before Stage 10.`

## Stage 9 closure — scouting, missions and mode flow

- **Acceptance 1–3 / C-04 / H-22–24:** Added `AiStage9SkirmishManagerV1`, a deterministic pure proposer registered after the Stage 7/8 managers. It creates stable reachable-frontier questions, uses only committed visible/last-seen permitted evidence, retains one contact/incident identity rather than inflating repeated sightings, and sends a legal scout or a last-seen search instead of claiming map coverage is success. Threat incidents are keyed by base/region and classified as worker, army, flyer, naval/transport or unknown with tick expiry/confidence. Squads persist duty/domain/objective/deadlines, retain a target-player focus across multiple opponents, keep an initial home reserve, and turn a 1,200-tick impossible assembly wait into a bounded smaller mission instead of permanent idle assembly.
- **Missions/domains / C-05–06 / D-04–05:** Local threats receive a bounded asset-relative defense before optional offense; distant bait is not a defense incident. Attack and defense actions require declared compatible target domains, so an observed flyer cannot be “countered” by a ground-only actor. Direct, air/naval and transport access are taken only from the committed Stage 8 graph; a transport route creates one Stage 8 child operation with a persisted squad handoff rather than an ad-hoc boat order. The profile's voluntary-offense budget remains the sole cap for voluntary attack missions; defense, scouts and transport child phases are separate lanes. A visible neutral can have one own-unit approach claimant, revalidated by permitted ownership each observation; allied players never receive orders.
- **Mode, authority and save:** The observation adapter now publishes public win/loss/tie configuration as bounded mode goals plus self-elimination state. The pure evaluator preserves actual mode completion/failure, recovery/hopeless tick age and a one-time concession identity. It proposes `concede` only after 1,200 uninterrupted hopeless ticks and routes it through `CommandBusService.dispatchAi` as the existing `CONCEDE` command; the existing `GameModeConditionChecker` and `ScoreTracker` remain the only result/score authorities. The state gains canonical `skirmish` and lifecycle-bearing squad slices, structural checks, legacy-V1 default migration and an explicit slice-policy owner, so reload cannot erase missions, incident expiry or a pending concession.
- **Debug/source routing:** `AiDebugSnapshotV1.skirmish` now exposes bounded questions, incidents, squad mission state, mode/concession reason and causal timeline entries. The existing **Enemy Intel & Scouting** panel renders those recorded facts without re-planning or a live hidden-world lookup. The Phaser source index records the Stage 9 manager and shared-dispatch rule for future agents.
- **Authored evidence (`authored_not_run`):** `ai-stage-9-skirmish-manager.spec.ts` covers C-04 question-driven scouting and no repeated-contact inflation, H-23 bounded partial launch, C-05/C-06 stable opponent focus and distant-bait protection, plus sustained one-shot concession. The Stage 15 real runtime suite must add/exercise the complete Normal land vertical loop, C-04/05/06, D-04/05, H-22/23/24, SEQ-01, income-under-minor-raid, applied attack/retreat/reinforcement/second-mission, actual neutral conversion/allied defense, island/air/naval, and authoritative score-screen variants. They are intentionally not labeled passed from these pure cases.
- **Omission Audit (manual only):** Re-read every Stage 9 requirement, H6/H7, C3/C4, D4/D5 and the Stage 9 vertical-slice gate. Repaired an initial nearest-visible-contact bias by preserving the selected target player; constrained defense to an asset-relative local radius and 200-tick leash; required target-domain compatibility; added a legal Stage 8 transport child rather than a disconnected sea order; appended new transport plans after their owner advances; and migrated old V1 saves safely. The final loop deliberately leaves advanced engagement scoring, spell/support execution, measured objective effects, detailed retreat/reinforcement and calibrated faction/map runtime behavior to their stated Stage 13/15 owners; none is presented as validated.
- **Final Closure Audit / deferred evidence:** Manually traced committed observation -> question/threat/mission/mode reducer -> Stage 6 arbitration/claims -> shared move/attack/concede commands -> authoritative mode application -> save/canonical/debug projection. Stage 9 is `implemented_unvalidated`: no tests, lint, type checks, builds, formatting validation, browser/runtime smoke, `git diff --check`, scenario runner, real skirmish, score scene or command application test was executed by explicit policy. Shared state/observation/debug contract additions invalidate earlier compile/serialization evidence; Stage 15 reruns Stages 2–9 plus the complete deferred ladder.
- **Publication / next stage:** This ledger is committed and pushed with the Stage 9 closure immediately after this update. Next: Stage 10 only, recommended `gpt-5.6-terra` / high. Resume prompt: `Implement Stage 10 only from docs/ai/759-skirmish-ai/00-start-here.md for #759. Verify the published Stage 9 SHA, then implement legal base identity, placement, expansion and economic-area planning through the committed observation/brain/command/debug seams. Author every Stage 10 test, scenario and runtime fixture, but do not run tests, lint, type checks, builds, formatting validation, browser/runtime smokes, git diff --check or equivalent validation. Perform manual acceptance, Omission and Final Closure audits, record implemented_unvalidated evidence, commit, push, verify the remote SHA, and stop before Stage 11.`

## Stage 10 closure — stable bases, placement candidates and expansion intent

- **Acceptance 1 / base identity:** Added definition-backed `AiObservedActorV1.mainBuilding`, projected only for owned actors, and `AiStage10BaseManagerV1`. It persists a base by main-structure actor ID, attaches only bounded nearby owned members, records access/anchor facts and distinguishes active, proposed, evacuating and lost state. A new main structure near a persisted proposed site adopts that expansion identity. The legacy `MapAnalyzer` now anchors its fallback placement center to a main structure and excludes mobile scouts/army from its legacy fallback average.
- **Acceptance 2–4 / legal candidate and reservation boundary:** The manager creates a bounded deterministic ring shortlist and one stable expansion-site reservation with stable site keys. The persisted contract includes expired/rejected site cooldown data for outcome reconciliation, rather than making tick-keyed duplicate requests; Stage 12/15 must connect actual rejection outcomes to that field. Candidate facts are intentionally advisory: `SharedCommandApplicationService.applyConstruct` remains the authoritative revalidation path for faction prerequisites, resources, whole spawned footprint, navigation, collision and builder assignment immediately before application. No private deletion, teleport, hidden terrain query or placement-stub shortcut was added.
- **Acceptance 5–7 / expansion and economic area:** The initial configurable profile-bounded logic records a resource-life/zero-throughput pressure trigger, favors a visible reachable distant resource, and creates at most one proposed expansion per player. A construct command is emitted only when the committed runtime capability catalog declares a builder can construct the observed main-structure type; otherwise the saved proposal exposes that unsupported capability instead of inventing a building. This preserves legal duplicate deposits/buildings as future economic-area demand work: it does not apply a false per-type uniqueness rule. Full island builder/protection/seat/landing/reinforcement execution remains coordinated with the existing Stage 8 transport lifecycle and is explicitly deferred to the Stage 15 runtime fixture because this stage has no confirmed faction expansion prefab in the catalog.
- **Acceptance 8 / debug:** `AiDebugSnapshotV1.bases` and the new **Bases & Placement** debug category display committed base lifecycle, anchor, member count, access, reserved site, rejection count and expansion trigger. The panel reads saved snapshot data only; it does not run a path/placement query.
- **Authored evidence (`authored_not_run`):** `ai-stage-10-base-manager.spec.ts` covers stable main-structure identity despite a distant scout plus one stable visible-resource expansion reservation. The Stage 15 manifest must execute PLACE-01–03, EXP-01–02, trapped production/spawn, mineral/drop-off access, depleted resources, interrupted builder, overlapping footprint, multi-base ownership, island establishment/evacuation, H-05/H-07/H-08/H-13/H-16/H-17/H-18/H-21/H-29 and SEQ-03/05 runtime variants against both real factions.
- **Omission Audit (manual only):** Re-read all Stage 10 requirements and PLACE/EXP cases. Traced definition metadata -> committed observation -> pure persisted base manager -> Stage 6 arbitration/claims -> `CommandBusService.dispatchAi` -> shared construction footprint revalidation -> outcome reconciliation -> canonical save/debug panel. Explicitly retained duplicate capacity legality and isolated the former averaged-mobile-actor legacy anchor. Remaining unimplemented portions are actual field-row placement, resource/drop-off throughput/service saturation measurement, complete footprint route simulation, outcome-driven rejected-site memory, expansion transport handoff and runtime island establishment; they are not claimed as complete and remain Stage 12/15 repair/validation obligations.
- **Final Closure Audit / deferred evidence:** Stage 10 is `implemented_unvalidated`. Reviewed registrations, exported symbols, saved canonicalization, migration-compatible optional fields, consumer/debug ownership and construct authority manually. No tests, lint, type checks, builds, formatting validation, browser/runtime smokes, `git diff --check`, or scenario runner were executed per user policy. The changed observation/state/debug contracts invalidate earlier compile/serialization evidence and Stage 15 must rerun the complete ladder. Host model/effort metadata was not exposed; documented owner recommendation is `gpt-5.6-terra` / high.
- **Publication / next stage:** This ledger is committed and pushed with the Stage 10 closure immediately after this update. Next: Stage 11 only, recommended `gpt-5.6-sol` / high. Resume prompt: `Implement Stage 11 only from docs/ai/759-skirmish-ai/00-start-here.md for #759. Verify the published Stage 10 SHA, then implement one bounded persistent fortification graph per base with legal connected wall/tower/stair placement, whole/incremental friendly clearance, deliberate gate slot, marginal coverage, breach recovery, save/debug seams and every Stage 11 WALL/H fixture. Author but do not run all tests, lint, type checks, builds, formatting validation, browser/runtime smokes, git diff --check or equivalent validation. Perform manual acceptance/Omission/Final Closure audits, record implemented_unvalidated evidence, commit, push, verify the remote SHA, and stop before Stage 12.`

## Stage 11 closure — connected fortification graphs and breach recovery

- **Acceptance 1–2 / persistent bounded graph:** Added and registered `AiStage11FortificationManagerV1`. It owns one stable graph identity per base, discovers short fronts only between committed blocked/water/map-boundary terrain anchors, reserves a deliberate center opening, limits each graph to 16 walls/3 towers/2 stairs and the difficulty placement quota, and rejects open-field/no-value layouts. Node identities, two connected side prefixes, dependencies, terrain anchors, protected assets, construction state and breach state persist in `AiBrainStateV1.fortifications`; an abandoned graph cannot be replaced by a fresh ID to evade cumulative limits.
- **Acceptance 3 / real budget and macro floors:** Runtime catalog entries now expose definition-derived resource costs. Before funding a new graph, the planner defers to unmet bootstrap-worker, supply-buffer and sustainable-food demands; it then subtracts ordinary reserved/obligated resources, requires two legal fortification builders while claiming only one, applies a 20% per-resource discretionary ceiling (30% for the persisted safe/defensive profile), counts every planned/finished graph across bases against the player ceiling, and emits matching resource claims. These are ceilings, not spending targets; shared application remains final affordability authority.
- **Acceptance 4–6 / topology, routes and idempotence:** The committed observation contains a bounded, player-permitted local construction-cell projection (maximum four bases/2,048 cells). Catalog construction profiles reproduce the runtime square footprint-radius rule plus definition navigation heights/ports and sight. Candidate footprints must be legal/non-overlapping; the hypothetical whole layout preserves nearby worker/resource/production/retreat access between protected side, opening and exterior. Because prefixes add a subset of the same blockers, the same proof covers incremental connectivity. Stable effect/site/actor claims, reservation-aware requested state, observed actor matching and terminal-outcome retry prevent accepted, reserved, under-construction or finished nodes from becoming duplicate side effects; uncertain retry reuses the same effect identity.
- **Acceptance 5 / towers, stairs, opening and domains:** Towers replace distinct end segments only when they add non-overlapping threat-lane sight coverage and record only definition-supported target domains. Each elevated component has a protected-side stair and explicit reachable defender post. With no gate capability the opening node is finished-empty and never emits construction; the same stable slot can accept a future gate-capable catalog object without replanning. Walls receive no air-defense credit, and any footprint within two tiles of a known shore transfer/staging point rejects the graph.
- **Acceptance 7 / breach response:** Missing previously finished actors create one persisted breach with bounded retry/cooldown. The manager rebuilds only eligible missing dependencies, moves an available ground/mixed defense squad through the ordinary shared move command toward the reachable protected side, and records one response effect. It abandons a breached graph when its protected value is no longer justified or after two failed recovery episodes; it never teleports defenders or restarts the entire graph.
- **Acceptance 8 / debug:** `AiDebugSnapshotV1.fortifications` and **Bases & Placement** now expose saved graph/node lifecycle, construction dependency, terrain anchors, protected count, whole/incremental connectivity, resource budget remaining, tower marginal/domain coverage, opening, defender-post reachability and breach recovery. The overlay converts saved logical positions only; opening the panel does not call placement/pathfinding or inspect hidden actors.
- **Authored evidence (`authored_not_run`):** `ai-stage-11-fortification-manager.spec.ts` authors WALL-01/02 deterministic bounded connected front/opening, H-27 open-field rejection, WALL-03 stair/post and domain-correct marginal towers, WALL-05 shore preservation, future gate-slot reuse, macro-floor deferral, partial-site sequencing, H-28 stable effect/reservation suppression, and WALL-04 bounded segment/stair rebuild plus defender movement. Stage 15 must execute these plus real Wall/WatchTower/Stairs footprint/height ports, full and every-prefix routes, hostile opening path/exposure, multi-base cumulative budget, air/naval coverage, shared application/replay/save/reconnect and both-faction runtime variants.
- **Omission Audit (manual only):** Re-read all Stage 11 requirements, retained acceptance, WALL-01–05 and H-27/H-28. Traced runtime definitions/topology -> fair bounded observation/catalog -> pure graph/reconciliation -> Stage 6 claims -> `CommandBusService.dispatchAi` construct/move -> shared application -> canonical save/debug. Repaired initial gaps in player-wide multi-base budgeting, macro-floor deferral, builder exclusivity/worker reserve, runtime-derived footprint radius/navigation ports, future gate reuse, nearby shore protection, occupied-collider projection, disconnected-post defender withdrawal, bounded failed-repair attempts, low-value breach abandonment, desired defender posts and reservation-aware duplicate suppression. The pure coverage score is a deterministic threat-lane approximation; Stage 15 still must prove actual hostile detour/exposure, every runtime prefix, legal elevated traversal and command application rather than treating authored graph facts as runtime success.
- **Final Closure Audit / deferred evidence:** Stage 11 is `implemented_unvalidated`. Manually reviewed authority, registrations, compatibility of optional catalog/map/graph fields, canonicalization, bounded work, debug cleanup and shared dispatch. No tests, lint, type checks, builds, formatting validation, browser/runtime smokes, `git diff --check`, or equivalent executable validation were run under the user policy. Changed contracts invalidate earlier compile/serialization evidence; Stage 15 reruns the complete ladder. Host model/effort metadata was not exposed; documented owner recommendation is `gpt-5.6-sol` / high.
- **Publication / next stage:** This ledger is committed and pushed with Stage 11 immediately after this update. Next: Stage 12 only, recommended `gpt-5.6-terra` / high. Resume prompt: `Implement Stage 12 only from docs/ai/759-skirmish-ai/00-start-here.md for #759. Verify the published Stage 11 SHA, then implement economy recovery, command-outcome-driven placement retry, anti-blocking and productive reassignment across the committed base/fortification/transport seams, including every Stage 12 REC/H/D fixture. Author but do not run tests, lint, type checks, builds, formatting validation, browser/runtime smokes, git diff --check or equivalent validation. Perform manual acceptance/Omission/Final Closure audits, record implemented_unvalidated evidence, commit, push, verify the remote SHA, and stop before Stage 13.`

## Stage 12 closure — causal recovery, anti-blocking and productive reassignment

- **Acceptance 1–2 / measured recovery and stable retry:** Added `AiStage12RecoveryManagerV1` and the canonical `AiBrainStateV1.recovery` slice. Each saved record has a stable causal key, domain, owner/actor, entered and last-progress ticks, bounded next retry, finite phase deadline, attempt, alternate and released claims. The manager uses the H1 200-tick backoff floor and 1,200-tick phase ceiling; it retains the original entered/progress timestamps so a renamed plan, new retry or explanation cannot erase failure age. It only creates shared AI intents and never teleports, deletes, directly mutates, or bypasses construction validation.
- **Acceptance 3 / observed blockers:** A visible hostile actor near a committed active base can receive one claimed compatible-defense-squad attack intent with an ordinary `target_visible` precondition. Remembered/unseen IDs are never targeted. The pre-existing scouting/knowledge owner remains responsible for investigating an unobserved access suspicion.
- **Acceptance 4–6 / economy, placement, repairs and transport boundaries:** Zero delivered income with an observed reachable positive source triggers one bounded reassignment of uncontained definition-backed gatherers; transport-owned cargo is not stolen. Terminal rejected/failed/cancelled expansion outcomes create a persisted site cooldown and clear the reservation so Stage 10 must choose another visible candidate. Owned durability is now a player-permitted `healthPermille` observation; repair triage is limited to `min(2, floor(workers/4))`, while the runtime shared repair path remains authoritative. Transport, fortification, squad and optional-plan records use the same terminal abandon/release mechanism, so a stranded passenger/breach/blocked retreat cannot keep provisional claims indefinitely.
- **Acceptance 7–9 / supervisor, authority and debug:** The pure brain releases only exact provisional claims named in an abandoned recovery record; it never treats a missing outcome as strategic defeat. Recovery state is migration-safe, validated and canonicalized. `AiDebugSnapshotV1.recovery` and **Logistics & Workers** expose cause, domain, attempt, next retry, deadline, alternate and released-claim count from committed state only. The capability manifest/source index now trace the Stage 12 boundary and the hardening ledger maps H1/H5 focused evidence to this owner.
- **Authored evidence (`authored_not_run`):** `ai-stage-12-recovery-manager.spec.ts` authors REC-01/H-16 productive gather reassignment, REC-02/H-01 causal-age/backoff suppression, REC-03 observed-only proxy clearing, REC-04 placement rejection cooldown and terminal optional release, plus WALL-04/DOMAIN-04/FIGHT-05 bounded transport/fortification/retreat recovery. Stage 15 must additionally execute H-01–05, H-16–18, D-04, WALL-04, DOMAIN-04 and FIGHT-05 through real command application, source depletion/regrowth, drop-off/tender/producer loss, supply recovery, stun/slow distinction, wall/shore/landing access, save/load and authority-change variants.
- **Omission Audit (manual only):** Re-read Stage 12, retained acceptance, WALL-04, DOMAIN-04, FIGHT-05, H-01–05, H-16–18 and D-04. Traced committed observation/outcomes -> pure recovery records -> Stage 6 arbitration/claims -> `CommandBusService.dispatchAi` -> shared application -> canonical save/debug. Confirmed duplicate capacity is still legal; suppression is stable-effect/site backoff only. Confirmed observed enemy target fencing, worker/cargo ownership, bounded record count and released-claim exactness. Unsupported live data (actual repair capability, stun/freeze duration and final path landing clearance) is not fabricated: Stage 15 runtime fixtures must prove the shared engine outcome.
- **Final Closure Audit / deferred evidence:** Stage 12 is `implemented_unvalidated`. Manually reviewed manager registration, migrations, structural validation, canonical ordering, projected debug, panel read-only behavior, base override ordering and exact reservation cleanup. No tests, lint, type checks, builds, formatting validation, browser/runtime smoke, `git diff --check`, scenario runner or executable validation was run by explicit user policy. The new observation/state contracts invalidate previous compile/serialization evidence; Stage 15 reruns the complete ladder.
- **Publication / next stage:** This ledger is committed and pushed with Stage 12 immediately after this update. Next: Stage 13 only, recommended `gpt-5.6-sol` / high. Resume prompt: `Implement Stage 13 only from docs/ai/759-skirmish-ai/00-start-here.md for #759. Verify the published Stage 12 SHA, then implement tactical squads, local combat preservation, support/heal/spell coordination and remaining debug completion across the committed recovery/transport/fortification seams. Author but do not run tests, lint, type checks, builds, formatting validation, browser/runtime smokes, git diff --check or equivalent validation. Perform manual acceptance/Omission/Final Closure audits, record implemented_unvalidated evidence, commit, push, verify the remote SHA, and stop before Stage 14.`

## Stage 13 closure — tactical squads, combat preservation, support and debug workbench

- **Acceptance 1 / squad and task-force lifecycle:** Added and registered `AiStage13TacticsManagerV1`. It refines Stage 9 missions through forming, assemble, rally, advance, engage, defend, regroup, retreat, recover and reserve phases; defense has first actor ownership priority, absent actors are released, mixed forces split into stable ground/air/water squads, and cargo remains transport-owned until handoff. Assembly/effect deadlines force a finite decision or recovery instead of permanent waiting. Persisted last-member/loss and useful-effect facts prevent mission resets from hiding attrition or inactivity.
- **Acceptance 2–3 / bounded engagement policy:** The pure local estimator uses observed effective health/armour, definition damage/cadence/minimum range, owned cooldown, high-ground range, arrival delay, area concentration, regeneration/healing and stun/slow penalties. It filters weapons by actual target domain and caps local contacts/pairs. The initial 1.2 engage, 0.8 retreat and middle regroup bands are saved with confidence and predicted losses; critical health, topology loss, mission deadlines and a persisted oscillation counter override unsafe relaunch loops. No hidden opponent cooldown, remembered HP or invented roster multiplier is consumed.
- **Acceptance 4–6 / objectives, positions and fire control:** A bounded visible objective portfolio scores enemy core, production, drop-off, gathering and combat value against distance and committed route status; a valid target changes only for at least 20% useful-score improvement. Stable scripts cover advance/focus, ranged distance, hold, air interception, naval control, landing regroup, protected retreat and rampart defend/reinforce/withdraw. Movement uses legal domain cells or ordinary target-following attack commands, formation candidates respect minimum range and harmful observed zones, and materially unchanged orders are suppressed. Persisted per-signature actor coverage drains oversized squad orders across the profile quota and records progress in the panel, preventing a large retreat/advance batch from being discarded and then forgotten. Damage reservations persist through expected arrival/impact, account for current health plus armour, spread fire after lethal coverage, and release on target/actor loss, expiry or authoritative failure; newly rejected reservations are removed after arbitration.
- **Acceptance 7–9 / support, effects and fortifications:** The observation now projects runtime spell research/readiness/autocast, owned healing cooldown, visible statuses and player-visible area zones in logical tile coordinates. Manual support selects a bounded legal in-range cluster, leaves autocast spells to the pawn owner, reserves caster/effect identity, discounts existing statuses and caps healing across pending heals at missing health. Support releases on terminal outcome/expiry; temporary support has an explicit lifetime. Reachable rampart positions are assigned distinctly, a 25% mobile reserve stays unposted, protected opening/post route identities are saved, and topology loss withdraws rather than trapping defenders.
- **Acceptance 10 / completed diagnostic surfaces:** `AiDebugSnapshotV1` now records squads, estimates, targets/alternatives, loss/effect progress, damage/support reservations, lane/cursor limits and honest why-not completeness. The panel adds **Squads & Support** and **Runtime & Limits**, recorded tactical/transport/fortification overlays, pagination, empty states, plan/effect drilldown and Shift+wheel read-only snapshot history. Historical rows never substitute current mutable state; omitted detail is labeled not retained and directs the developer to capture a bundle. Legacy wall-clock/blackboard calculations are no longer routed by active planning categories. History has a bounded explicit JSON export API.
- **Debug workbench / H9:** Added `AiIncidentCaptureStoreV1` with one capture per causal episode, five-entry eviction, configurable 64 MiB default quota, clone-on-read and disposal. The Stage 5 runner now exposes named safe breakpoints, one-decision stepping and isolated pure what-if comparison without mutating captured state/RNG. Runtime bridge capture retains only bounded host-confidential manifests automatically. Optional proposer exceptions are isolated into an unevaluated recorded proposal, while other lanes continue; `findAiWhyNotExplanationV1` returns exact recorded rejection/unresolved data or explicit `not_recorded`.
- **Authored evidence (`authored_not_run`):** `ai-stage-13-tactics-manager.spec.ts` explicitly covers assembly -> rally -> advance, quiet defense, uncertain regroup, losing retreat, deadline recovery, sole actor ownership/straggler release, transport-owned cargo -> squad handoff/landing regroup, useful-effect progress, casualty accounting, expected-impact overkill prevention, target hysteresis/order suppression, retreat/relaunch oscillation damping, economy/production raids, missing-health healing, manual versus autocast spells, air targeting, air/naval domain splitting, rampart posts/mobile reserve, breach reinforcement, topology-loss withdrawal and finite assembly/effect deadlines. `stage-13-tactics.json` registers DOMAIN-06, RAID-01/02, WALL-03, FIGHT-01/02/04, H-23–25/29/30 and C-06 as pure `authoredFixture` cases; the matrix includes the tactical spec only for these rows and rejects runtime mode until Stage 15 supplies actual runtime fixtures, so authored evidence cannot masquerade as end-to-end proof. RAID-03, FIGHT-03/05, H-26 and H-32 remain explicitly unregistered because their simultaneous-front, blocker/status, hostile-landing and second-match lifecycle evidence requires broader/runtime drivers. `ai-brain.spec.ts`, `ai-repro-cli.spec.ts` and `ai-incident-capture-store-v1.spec.ts` add DBG why-not, proposer isolation, what-if immutability, retention/quota/disposal and inert-label cases. Stage 15 must execute these and complete real runtime RAID-01–03, FIGHT-01–05, DOMAIN-06, C-02/05/06, D-05, H-23–26/29/30/32, status/zone/projectile, elevated traversal, transport handoff, save/replay and continuous-match variants for both factions.
- **Omission Audit (manual only):** Re-read Stage 13, retained acceptance, strategic-objective obligations, packet 12 and referenced scenario rows. Traced effective definitions/components -> fair committed observation -> Stage 9 squads/Stage 13 pure refinement -> Stage 6 arbitration and post-arbitration reservation/order cleanup -> `CommandBusService.dispatchAi` -> shared attack/heal/spell/move application -> command outcomes -> canonical save/debug/history/capture. Repaired gaps found during review: Stage 9 defense/attack actor overlap, tactical command dispatch, enemy-domain weapon filtering, arrival-aware impact identity, pending-heal overcommit, support rejection cleanup, missing lifecycle phases/deadlines, expired-deadline permanent recovery, oversized squad-order loss, batch effect identity, unposted mobile reserve, visible-zone coordinate/visibility policy, combat-profile canonical ordering, player-zero debug refresh, historical/current snapshot mixing and mutable legacy panel routing. Strategic terrain/breach candidates remain represented through saved fortification/route mission facts rather than fabricated attackable actors; Stage 15 must establish actual gameplay effectiveness.
- **Final Closure Audit / deferred evidence:** Stage 13 is `implemented_unvalidated`. Manually reviewed registrations, contracts, migration-compatible optional boundaries, canonical ordering, finite-number guards, one-owner rules, profile quotas, runtime dispatch, outcome cleanup, snapshot-only active debug consumers and test/coverage ownership. No tests, lint, type checks, builds, formatting validation, browser/runtime smoke, `git diff --check`, scenario runner or equivalent executable validation was run under the explicit user policy. These broad observation/state/debug changes invalidate earlier compile/serialization evidence; Stage 15 reruns the complete ladder. Host model/effort metadata was not exposed; documented owner recommendation is `gpt-5.6-sol` / high.
- **Publication / next stage:** This ledger is committed and pushed with the Stage 13 closure immediately after this update. Next: Stage 14 only, recommended `gpt-5.6-terra` / xhigh. Resume prompt: `Implement Stage 14 only from docs/ai/759-skirmish-ai/00-start-here.md for #759. Verify the published Stage 13 SHA, then implement evidence-backed composition adaptation, real research/upgrade scoring, supported archetype behavior, difficulty lifecycle integration and final component-coverage closure across the committed tactical/debug seams. Author but do not run tests, lint, type checks, builds, formatting validation, browser/runtime smokes, git diff --check or equivalent validation. Perform manual acceptance/Omission/Final Closure audits, record implemented_unvalidated evidence, commit, push, verify the remote SHA, and stop before Stage 15.`

## Stage 14 closure — adaptive composition, research, profiles and difficulty provenance

- **Acceptance 1–3 / fair adaptation and useful composition:** Added `AiStage14AdaptationManagerV1` after macro/tactics. It records only visible current enemy capability evidence, keyed to the observed contact and permitted facts. The same contact can establish two consecutive evaluations but cannot add invented mass or corroboration. Confirmed flyer, water/container, area-damage and static-fortification evidence produces only catalog-backed anti-air, water-control, ranged/support or legal alternative-role demand. Ready actors and accepted effect reservations count separately; a real deficit can still produce legitimate duplicates, while unresolved accepted work cannot cause repeated queue spam. The manager never cancels committed production.
- **Acceptance 4 / legal technology boundary:** The Phaser observation adapter projects an immutable candidate only when the owned live `ResearchComponent` says it is legal, using the runtime definition's resource cost, time, refund and unit/spell benefit. Stage 14 scores current and likely 1,200-tick beneficiaries against cost, queue delay and visible-threat survival cost, reserves producer/resource/effect claims and dispatches the shared `RESEARCH` command. Legacy behavior-tree upgrade entry points are fenced when a pure skirmish brain is active, leaving the pure planner as the single research-launch authority.
- **Acceptance 5–7 / personalities and lifecycle:** Deterministic opening selection now names balanced, rush, macro, turtle and tech branches. Shared essential worker/supply/prerequisite recovery remains mandatory; rush lowers the first safe force, macro raises expansion utility, turtle is recognized by justified fortification policy, and tech raises a legal-upgrade score. Saved air-control/naval/expeditionary identities are capability/map/transport gated and fall back once, deterministically and with a reason, to balanced; an ordinary threat never rerolls a supported opening. Adaptation evidence, targets, cooldown, research rationale and cancellation policy are migrated and canonically saved.
- **Acceptance 8–9 / component closure and explainability:** The shared observation, state, migration, serialization, structural guard, capability coverage manifest, Stage 6 state-patch boundary and read-only Production & Tech panel now carry the Stage 14 contract. The debug snapshot exposes evidence, confirmed target roles, transition reason/tick, research score/type, archetype and retain-commitment policy. The manager uses a narrow `adaptationDemands` projection so it cannot overwrite the same-tick Stage 7 opening/supply ledger; the authored brain-boundary case records that invariant.
- **Authored evidence (`authored_not_run`):** `ai-stage-14-adaptation-manager.spec.ts` authors TECH-01–04, PRO-06/07 and DOMAIN-01/02/D-05; profile defaults authors D-02/D-03; the controller static spec authors the single-research-authority fence; and `ai-brain.spec.ts` authors the macro/adaptation ledger merge. `stage-14-adaptation.json` registers PRO-06/07, TECH-01–06, SCOUT-05, STRAT-05/06/09, DOMAIN-01/02/05, C-01/02/04, D-02/03/05 and H-31/32. `difficulty-calibration-v1.json` is the prepared D-06 manifest: paired seeds/maps/factions/opponents across difficulties, pinned baseline, fair-rule invariants, telemetry, confidence reporting and fail-closed runtime evidence. These authored files do not count as runtime proof.
- **Omission Audit (manual only):** Re-read Stage 14, retained C/D/H acceptance, Stage 7/10/11/13 contracts and the user’s duplicate-production requirement. Traced visible runtime definitions/components -> immutable research/evidence observation -> pure Stage 14 role/research proposals -> Stage 6 claims/reconciliation -> `CommandBusService.dispatchAi` -> shared research application -> resolved effective actor levels -> save/canonical/debug projection. Repaired manual findings: invalid stage effect identities, a same-tick macro-ledger overwrite risk, missing accepted-counter accounting, unbounded legacy research authority, macro expansion differentiation and the missing D-06 calibration artifact. No hidden enemy composition, unavailable producer, fictitious counter, arbitrary cancellation or difficulty privilege enters the proposal.
- **Final Closure Audit / deferred evidence:** Stage 14 is `implemented_unvalidated`. Manually inspected registrations, consumers, migration defaults, canonical ordering, claim identity, fair observation boundary, shared research dispatch, legacy fence, manager ordering/merge behavior, debug projection/panel ownership, authored fixture registration and coverage-manifest ownership. No tests, lint, type checks, builds, formatting validation, browser/runtime smoke, scenario runner, `git diff --check` or equivalent executable validation was run by explicit policy. The expanded shared contracts invalidate prior compile/serialization/runtime evidence; Stage 15 must rerun the complete ladder, including real level/cargo/vision/attack effects, all factions/maps, save/replay/host migration/second-match disposal, command outcomes and D-06 paired calibration.
- **Publication / next stage:** This ledger is committed and pushed with the Stage 14 closure. Next: Stage 15 only, recommended `gpt-5.6-sol` / xhigh. Resume prompt: `Implement Stage 15 only from docs/ai/759-skirmish-ai/00-start-here.md for #759. Verify the published Stage 14 SHA, then run the complete deferred validation ladder for Stages 0–14: authored pure tests, contract/serialization/migration, focused gameplay, command/server/replay, fixture runner, lint/typecheck/build/assets/editor/repository checks, browser/runtime skirmishes, save/replay/host/continuous-match matrix and pinned-baseline D-06 calibration. Repair task-owned failures and rerun affected evidence. Complete formal code review, docs/skill learnings, final closure and remote verification; do not begin Stage 16.`

## Cross-stage hardening coverage

For H1–H9 and each H/SEQ ID record: stage owner, implemented symbols, debug fields, save/cleanup owner, focused command/result, final command/result, source/config/fixture digest, and unresolved causes. Claim scope, useful-progress predicate and deadline must be explicit. Use packet 09's stage table and packet 10's cases; do not mark coverage from a trace label alone.

Stage 2 now supplies the typed `AI_HARDENING_COVERAGE_V1` ledger and validates that H-01–H-32 are each owned exactly once. The unchecked boxes below continue to represent completed runtime behavior and final evidence, not contract availability.

Stage 3 implements the H3 authority/reconciliation slice and authors H-06–10 coverage, but the H3 box remains unchecked until Stage 15 executes the required real-runtime and recovery evidence.

- [ ] H1 measured progress and non-resettable causal recovery
- [ ] H2 reservation dependencies, atomic grants and cycle recovery
- [ ] H3 authoritative reconciliation, pending backlog and epoch fencing
- [ ] H4 fair lane service and bounded emergency preemption
- [ ] H5 useful sustainable economy, spawn/capacity and exposure
- [ ] H6 complete repeated offensive missions and safe reinforcement
- [ ] H7 fair observations, query isolation and actual access clearance
- [ ] H8 transport handoff and measured fortification value
- [ ] H9 technical fault isolation, saved debt and next-match cleanup
- [ ] H-01–32 fault/progress cases and SEQ-01–08 continuous matches

## Final verification ledger

The classic-RTS/difficulty/debug review adds mandatory [C/D](11-classic-rts-and-difficulty.md) and [DBG](12-debug-workbench.md) ownership. For each ID use the same symbol, persistence, focused/final evidence fields as H/SEQ. The 121-case catalog and [interactive overview](overview.html) are authored requirements/explanation, not implemented or passing AI tests.

- [ ] C-01–06 opening/timing/army demand/scout evidence/pursuit/opponent focus
- [ ] D-01–06 fair distinct difficulty, persistence, safety and paired calibration
- [ ] DBG-01–06 why-not, capture/replay/step/diff, privacy and bounded lifecycle

- [ ] Every deterministic strategic scenario ID and positive/negative variant: authored, exercised, semantic and replay assertions, real-runtime counterpart or explicit future-capability status
- [ ] Legitimate duplicate production/deposits/units, advance throughput planning and suppression only after fulfilled demand
- [ ] Pure decision/serialization tests and full gameplay library target
- [ ] Command/server authorization, replay/application, schema migration
- [ ] Real simulation scenarios: macro, combat, farming/support, transport, fortification, modes
- [ ] Debug projection/panel/overlay correctness and UI-on/off determinism
- [ ] Save/load, reconnect, host migration, pause/speed and render-rate variation
- [ ] Seed/map/faction/difficulty/archetype matrix versus pinned baseline
- [ ] Formatting, lint, type checking, builds, assets/editor/repository checks
- [ ] Browser and human-facing match/score/debug workflows
- [ ] Formal code review, omission audit, fixes and reruns
- [ ] Durable documentation and applicable repository skill learnings
- [ ] Final closure audit, remote SHA/PR verification
