/** Presentation state calculated by the campaign overview for one chapter card. */
export interface CampaignChapterCardState {
  completedMissions: number;
  totalMissions: number;
  isRecommended: boolean;
  isLocked: boolean;
}
