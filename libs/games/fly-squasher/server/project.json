{
  "name": "fly-squasher-server",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/games/fly-squasher/server/src",
  "projectType": "library",
  "tags": ["scope:fly-squasher", "type:server"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/games/fly-squasher/server/jest.config.cts"
      }
    }
  }
}
